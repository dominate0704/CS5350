test = readTxt('phishing.test');
test_result = test(:, 71);
dev = readTxt('phishing.dev');
dev_result = dev(:, 71);
err_test = 0;
err_dev = 0;
majorTest = mode(test_result);
majorDev = mode(dev_result);
t = length(find(test_result == -majorTest));
disp("Majority baseline accuracy on test set is " + 100 * (1 - (t / length(test_result))) + "%.");
d = length(find(dev_result == -majorDev));
disp("Majority baseline accuracy on development set is " + 100 * (1 - (d / length(dev_result))) + "%.");